#Aufgabe: Deskriptive Statistiken 
#
#a) Analysiere die Verteilung der Zylinderanzahl (cyl), Getriebetyp (am) und Motortyp (vs) im "mtcars" Datensatz. Der Datensatz "mtcars" ist in R vorhanden und kann über data(mtcars) geladen werden. 
#
#Berechne die absoluten Häufigkeiten, die relativen Häufigkeiten und die kumulierten relativen Häufigkeiten für "cyl".
#Berechne die absoluten Häufigkeiten, die relativen Häufigkeiten und die kumulierten relativen Häufigkeiten für "am".
#Berechne die absoluten Häufigkeiten, die relativen Häufigkeiten und die kumulierten relativen Häufigkeiten für "vs".
#Interpretiere die Ergebnisse. Lasse dir dazu die Übersichtstabellen ausgeben. 
#
#Praxistipp
#
#
#Nutze die "table()", "prop.table()" und "cumsum()" Funktionen in R, um die verschiedenen Häufigkeiten zu berechnen.
